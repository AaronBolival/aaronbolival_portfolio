AJEB Portfolio V2

Run using VS Code Live Server. Import data/portfolio-data.json into the Firebase Realtime Database root if needed. The admin editor now uses labeled fields, date controls, checkboxes, nested groups, and repeatable item controls instead of raw JSON.
